# Node
node_modules/
npm-debug.log*
yarn-error.log

# package-lock.json is deliberately NOT ignored. build-release.sh runs
# `npm ci`, so the lockfile is what makes two builds of the same tag produce
# the same bundle.

# Build artifacts
admin/js/dist/
*.map
build/
releases/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.*.local

# Temporary
*.tmp
*.temp
.cache/
